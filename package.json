{
  "name": "strong-zero-css-art",
  "version": "1.0.0",
  "description": "CSS art of a Strong Zero can",
  "main": "index.js",
  "scripts": {
    "build": "sass scss/main.scss:css/main.css",
    "watch": "sass --watch scss/main.scss:css/main.css",
    "serve": "live-server --port=8080",
    "start": "live-server --port=8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Barrett Otte",
  "license": "MIT",
  "dependencies": {
    "sass": "^1.54.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
